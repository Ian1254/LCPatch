# LCPatch 1.2.1-beta.10

這個測試版集中修正 beta.9 的底欄事件吞掉、膠囊幾何、狀態頁返回變暗與頂欄模糊過重問題。

## 懸浮底欄

- 移除切換期間的整體輸入鎖定；新的點擊或拖曳可以直接接管尚未完成的膠囊動畫，不再因為前一次切換尚未結束而整次被忽略。
- 非目前選中膠囊的點擊不再用 `touchSlop` 當作是否有效的最後門檻；放開時一定會解析目標項目，避免手指有小幅位移就出現「動畫沒跑、頁面也沒跳」的情況。
- 拖曳仍只允許從目前可見的選中膠囊開始，且頁面依舊在放開後才切換。
- 連續快速切換時，新的 settle 會取消前一個尚未完成的 settle，讓膠囊與頁面目標保持一致。

## 膠囊幾何

- 修正 beta.9 對水平 inset 的重複計算。外層底欄仍為 248 × 62 dp，三個項目維持 80 dp 寬並保留 4 dp 外層水平 padding。
- 選中膠囊靜止寬度改為完整 80 dp、高度 54 dp，因此最左與最右位置到外框、以及上下方向都維持 4 dp 的視覺間距。

## 已啟用狀態頁

- 關閉全螢幕狀態 overlay 所在 Dialog 的背景 dim，避免收合尾端先看到被系統 Dialog 暗化的原卡片，再在 Dialog 卸載時突然變亮。
- 保留原本的反向 morph、來源卡片交接與兩幀卸載緩衝。

## 頂欄與模糊

- `LayerBackdrop` 不再預先鋪一層不透明 surface 後才擷取內容，避免即時模糊被底色吃掉後看起來像一整塊灰色／黑色頂欄。
- 頂欄表面漸層透明度降低，模糊半徑由 36 降至 28，讓最外層頁面的空白頂欄更接近由內容向上自然淡出的模糊過渡。
- 關閉模糊時仍保留同一套漸層退場，不會退回突兀的實色矩形。

## 驗證範圍

預發布流程包含單元測試、Lint、原生元件建置、Release APK 組裝、APK 簽章及簽章驗證。
