# LCPatch 1.2.2-beta.4

本測試版依時鐘 App 的底欄效果整理導航、漢化卡片和操作提示。

- 浮動底欄改成霧面背景、細邊線及中性灰選取塊；圖示與文字保持清晰，不再對底欄做背景模糊。
- 按壓時選取塊略微縮小，點按其他分頁時先預覽選取塊，頁面仍在放開後切換。拖曳時兩端以彈簧跟手並保留短暫尾端延伸，放開時從畫面上實際位置接續切頁或回位。
- 本機漢化的套用狀態與 PUA 資訊改用中性文字；下載項目的作者與來源也改為次要文字色，藍色集中在主要操作與進度。
- 錯誤不再展開在清單頂端或顯示操作按鈕；底部只短暫顯示摘要，完整訊息記錄在應用程式日誌。

底欄的手勢時序與不同裝置上的實際流暢度仍需實機檢查。
