# LCPatch 1.2.0-beta.9

本版依最新操作錄影重新收斂導航動畫，重點是不再加入額外形變或淡入淡出，而是讓主導航與子頁使用更一致、乾淨的水平移動語言。

## 底欄

- 移除 beta.8 的膠囊拉伸／收縮效果；選中膠囊恢復固定寬度，只做水平位移。
- 保留「拖曳時只預覽底欄選中位置、放手後才切換頁面」的互動模型。
- 浮動底欄仍只允許從目前選中的膠囊開始拖曳，頁面內容區不直接接管主頁左右切換。
- 圖示與文字仍依拖曳位置連續更新選中程度，但不再改變膠囊形狀。

## 頁面轉場

- 子頁進場與返回改為完整寬度的水平 slide，不再使用淡入／淡出。
- 移除舊版父頁只移動約 1/4 寬度的 parallax；進場與返回改為兩個完整頁面直接交接。
- 頂欄、頁面內容與底欄共用同一個導航 target、方向與 320 ms 時長，避免標題先切換、內容後移動的不同步感。
- 進入下一層時由右往左交接；返回上一層時完整反向，讓 push / pop 成為同一套動畫的正反方向。
- 主頁之間仍由 Pager 完成完整水平切頁；浮動底欄的拖曳本身不直接拖動 Pager。

## 開發與發布流程

- 版本號集中到 `version.properties`，避免一般 Gradle 設定變更誤觸發布。
- Android CI 改為 PR／手動驗證，不再在合併 `main` 後重跑相同 CI。
- CI 與 Release 共用同一個本地 Android build action，Actions 頁仍只保留 CI 與 Release 兩個頂層 workflow。
- 後續版本固定採用單一 release branch → 單一 PR → PR CI → squash merge → 版本檔觸發 Release 的流程。

## Beta 提醒

本版仍為預發布版本。更新後建議優先測試：底欄慢速拖曳、點擊切頁、設定頁進入各子頁、返回上一層，以及連續快速進出子頁時的頂欄／底欄同步狀態。
