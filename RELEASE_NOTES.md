# LCPatch 1.2.1-beta.17

這個測試版修正 beta.16 頂層場景重構造成的底部黑塊、異常頂欄及缺少頁名，並實際調整環境狀態卡動畫。

## 頂層頁面

- 移除 Pager 頁面內的巢狀 Scaffold。
- 完整頁面保持全螢幕，底欄改為覆蓋顯示；只有列表內容保留底部安全留白。
- 頂欄改為 PageScene 內的覆蓋層，與頁面內容一起水平切換。
- 頁名改用固定置中文字，不再依賴未連接的頂欄捲動狀態。
- 模糊來源與輸出拆成同一場景內的穩定兄弟層，降低慢速滑動重新取樣閃爍。
- 固定毛玻璃模糊半徑調整為 20，深色表面覆層提高至 82%。

## 環境狀態卡

- 展開與返回改為彈簧動態，不再沿用 beta.16 的固定時間 tween。
- 返回最後 8% 主動交接來源卡片，避免 Dialog 卸載後才突然顯示原卡。
- 保留成功綠色及操作按鈕區隔。

## 相容性

- 繼續保留 Limbus Company 1.114.0 支援。
